import { ScrollView, Text } from "react-native";
import { Screen } from "../../../components/Screen";

export default function TeamPage() {
  return (
    <Screen>
      <ScrollView>
        <Text className="text-black text-xl font-bold mb-6">Equipo</Text>
      </ScrollView>
    </Screen>
  );
}
